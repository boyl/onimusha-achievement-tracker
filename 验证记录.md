# 0.4.0 验证记录

日期：2026-09-13。Windows Steam 正式版 build 24769601、REFramework TDB 82、PowerShell 7.6.4。实机分辨率 2560×1440。

## 本版改动

- 按游戏文字语言自动选择简体中文或英文；游戏选择繁体中文时也使用本 Mod 简体中文文本，其余语言默认英文。
- 覆盖菜单、52 项成就名称及条件、原生物品／任务／地图名称、参考提示、错误提示及地图标签。保持中英文成就搜索。
- 字体改为随包分发的 Onimusha Tracker Sans（Noto Sans SC 的重命名 400 字重静态实例），完整保留 SIL OFL 1.1。没有打包微软雅黑或其他微软字体。
- 修复旧偏好中的 false 值被默认 true 覆盖的问题，影响小面板、未解锁筛选、缺失筛选及地图追踪。
- 修正清水寺百秽的位置：不再按敌人种类选择第一个同类实例，而是使用清水寺任务完成后生成、具有独立战斗区和特殊参数的对应配置 GUID。
- 提供根目录直接为 reframework 的 Nexus 安装包，以及中英文安装、使用和发布说明。

## 实际语言切换证据

首次测试发现，游戏文字语言切为英文后，via.ResourceManager.get_Language 仍返回 13（中文），追踪器未切换。用户明确报告“切换英文后，还是中文显示”。该入口已退役，未作为最终语言来源。

有界只读检查确认：app.savedata.cOptionParam.getTextLanguage 返回 1（英文）、getVoiceLanguage 返回 13（中文），GUI 的语言请求也为英文。正式适配器改为从 SaveDataManager.get_SystemSaveData 的 _Option 读取 getTextLanguage，独立于语音和资源管理器语言。没有调用语言设置或保存方法。

修复后正式 0.4.0 状态为 ready、language=en、game_language=English，地图标签为 Eastern Kyoto，无读取及 UI 错误。用户随后确认英文显示及切回中文“正常了”；最终状态为 language=zh、game_language=SimplelifiedChinese，同样无错误。此为真实游戏设置切换，区别于下面的模拟输入检查。

## 自动与运行时检查

- 13 个 Lua 文件通过 Lua 5.4 语法检查。
- 进度增减、单次事件归零、未知计数、重复条目、搜索、转场／错误清空、保存偏好等回归通过。
- 所有已知游戏语言名称及未知值的选择策略通过：两种中文设置→简体界面；英文及其他语言→英文。
- 全部静态 UI 翻译、成就英文名称／条件和地标文本均有对应英文，未混入中文；中英切换保留已选成就和偏好。
- 新增精确回归：文字为英文、语音仍为中文时必须显示英文；替身不提供 ResourceManager，误读该入口会失败。另验证切换中文／日文时清空名称缓存及英文回退。
- 在运行游戏内用独立控制器注入简体中文、英文、日文、法文和繁体中文语言输入，每种读取 52 项成就及 2,864 个文本样本，核对物品、任务和地图标签。日文／法文回退为纯英文；这些输入模拟没有改变游戏语言、用户选择或存档。
- 最终运行时检查的 701 个不同字符均被字体 cmap 和实际加载字形范围覆盖。
- 18/22/26 字号 × 1280/1920/2560 宽度的中文和英文文本换行检查通过。其他分辨率的完整视觉外观未实机验收。
- 地图虚拟→物理坐标、宽屏偏移、区域／楼层隔离、边界裁切、无效数值检查通过。此前用户已确认圆环随地图缩放和移动。
- 隔离中文目录安装和卸载 15 个 payload 文件通过，保留其他 Mod、配置及存档；中途失败回滚和路径越界拒绝通过。
- Nexus 包按精确文件清单构建，不包含安装器、开发探针、个人设置、游戏资源、REFramework DLL 或其他 Mod。检查压缩完整性，并比较源码、已安装文件、压缩包内容的 SHA-256。

## 定位证据与边界

全部 23 本杂记、36 只狛犬及材料点来自游戏配置；225 个物品节点的 GUID、物品 ID 与坐标经过运行时交叉核对。6 件武具可在普通地图定位，两刃【风卷】所在独立场景没有普通地图。

清水寺百秽修正为 Area201_002、GUID da71b42c-bcf1-4c91-b7fe-52659073924e。其世界位置为 (-42.1392, 2.5342, -138.3848)。原错误实例位于 Area204_005（地下研究所）。新增断言限定区域与实例；正式映射在中文显示“清水寺”，英文显示“Kiyomizu-dera Temple”。没有逐点现场战斗验证。

救援为 33 个候选遭遇点，成就仍使用 30 次原生救援目标；商人有洛东 28 个搜索参考点，不保证刷新。材料可能已拾取，也有商店、掉落和奖励等其他来源。一处棉织物点没有普通地图映射，面板明确提示未覆盖数量。

原生图标去重分支此前已在当前地图的真实启用图标上验证：独立控制器的定位状态由 visible 变为 native_object，游戏写入为 0；不等同于逐只狛犬现场验收。

没有逐点走访全部位置，没有修改存档来模拟收集，没有测试试玩版、其他游戏版本或非 Steam 运行时。手柄导航、Vortex 部署及所有楼层的实地体验尚未验证。

## 复现与交付

开发备份包内可用 Python 3 安装 tests/requirements.txt，然后运行 python tests/test_tracker.py。Nexus 安装包本身无需 Python 或 PowerShell。

正式状态文件：reframework/data/onimusha_achievement_tracker_status.json。偏好文件：onimusha_achievement_tracker_config.json。旧位置缓存保留但不再读写。临时诊断在交付前停止并停用，不包含在发布包中。

本记录描述代码与安装包验收，不作为远端发布证明。最终发布包必须由已推送的 Git 提交构建；BUILD_INFO.json 记录来源提交，发布后的页面和附件由独立发布记录核验。

## 0.4.1 验收（2026-09-13）

用户实机确认：传送及救援后的读取恢复正常；救援全图圆环、杂记全图与缺失筛选、地图光标聚焦名称均正常。热加载后状态 ready，errors=0、ui_error 为空，地图多点 visible，实际聚焦到幻魔杂记【罗掌愿】。完整 Lua 回归测试通过，新增场景数组独立读取、区域楼层隔离、缺失筛选、最近点聚焦测试。保留 0.4.0 包作为回滚版本。
